# chromadose frontend (pending design import)

This directory will hold the **Chromadose Web App** UI once the Claude Design
project is imported into the workspace (via "Send to Claude Code Web" or by
adding the `.dc.html` file here).

The UI is a static single-page app that calls the backend API in `../backend`
(see `../backend/README.md` for the endpoint list). Nothing to build here yet.
